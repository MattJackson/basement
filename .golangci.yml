version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - errcheck
    - staticcheck
    - revive
    - ineffassign
    - unused

  exclusions:
    paths:
      - internal/web/dist
      - frontend
