version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - errcheck
    - revive
    - ineffassign
    - unused

  exclusions:
    paths:
      - internal/web/dist
      - frontend
