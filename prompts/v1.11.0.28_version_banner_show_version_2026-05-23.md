You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui.

# Cycle: v1.11.0.28 — NewVersionBanner shows the available version

**Operator request**: banner currently says "A new version of basement is available. Refresh to load it." — too generic. Should say "Basement v1.X.Y.Z now available — Refresh" with the actual version.

## Backend changes

Find the middleware that emits `X-Build: <commit-sha>` on every API response (`internal/api/server.go` or middleware/ directory).

Add `X-Version: <tag>` alongside it. The version comes from the git tag at build time, stamped via ldflags or build-time env (mirroring how __BUILD_COMMIT__ gets stamped on the frontend side). If a version-stamping mechanism doesn't exist yet, the simplest add: at build time `git describe --tags --always` → exposed as a package-level constant → middleware reads it. If no tag exists, fall back to commit SHA short form (`X-Version: <short-sha>`).

Verify the response shows both headers:
```bash
curl -sI http://localhost:8080/api/v1/version | grep -i "x-"
# expect: X-Build: <sha>
# expect: X-Version: v1.11.0.28 (or current tag)
```

## Frontend changes

`frontend/src/shared/api/buildWatcher.ts`:
- Capture `X-Version` from server responses (in `observeResponse`)
- Store as module-level `serverVersion: string | undefined`
- Expose via subscriber callback so banner gets it: change `subscribe(fn: (m: boolean) => void)` to `subscribe(fn: (state: { mismatched: boolean; serverVersion?: string }) => void)`
- Heartbeat polling continues; latest serverVersion always reflected

`frontend/src/shared/ui/NewVersionBanner.tsx`:
- Subscribe receives new shape
- Display: `Basement ${serverVersion} now available — Refresh` (or graceful fallback to current generic text if serverVersion is undefined)
- Keep the Refresh button behavior

## Tests

Update `frontend/src/shared/api/__tests__/buildWatcher.test.ts`:
- Subscriber receives serverVersion when X-Version present
- Subscriber state shape change reflected (backward-compat: pass through `mismatched`)

Update / add NewVersionBanner test (vitest):
- Shows version when present
- Falls back to generic when not

## Read first

- `frontend/src/shared/api/buildWatcher.ts` (modify)
- `frontend/src/shared/ui/NewVersionBanner.tsx` (modify)
- `internal/api/server.go` (find X-Build middleware; add X-Version next to it)
- The cmd that builds the backend (find ldflags or stamping mechanism for __VERSION__)

## Hard constraints

- `pnpm build` + `go test -race ./...` green BEFORE push (run SYNCHRONOUSLY)
- `git commit <path>` pathspec form
- NO Co-Authored-By, NO `--no-verify`, NO emojis
- ONE commit per [[feedback_squash_commits]]
- Per [[feedback_freshman_must_merge_back_to_main]]: push HEAD:main FIRST, verify, then tag

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v1.11.0.28
git push origin v1.11.0.28
git ls-remote origin v1.11.0.28
```

## Acceptance

1. Backend emits X-Version on every /api/v1/* response (verified via curl)
2. buildWatcher captures + exposes serverVersion to subscribers
3. NewVersionBanner displays "Basement v1.X.Y.Z now available — Refresh" when version known
4. Tests cover both states (version known, version unknown)
5. Tag + main same commit; both pushed

Return summary AFTER both pushes succeed.
