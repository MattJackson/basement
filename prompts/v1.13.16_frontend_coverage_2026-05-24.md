You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui.

# Cycle: v1.13.16 — Frontend test-coverage backfill

v1.13.13/.15 backfilled Go coverage. Now do the FE.

## Approach

1. Generate baseline: `pnpm vitest --coverage --run 2>&1 | tail -100 > /tmp/fe-cov.log`
2. Identify components / hooks / utilities below 80%
3. Pick the highest-impact 3-5 modules (user-visible, frequently-rendered, high-blast-radius)
4. Write vitest tests covering:
   - User-driven interactions (click handlers, form submissions, modal flows)
   - Error states (failed fetch, validation errors)
   - Edge cases (empty list, single item, many items)
   - Hook return shapes (loading, error, success, refetch)
5. Re-measure coverage per module; target ≥80%

## Quality bar

- Use Testing Library patterns (no `shallow` rendering, no enzyme — vitest + RTL only)
- Mock React Query hooks via the project's existing pattern (look at how v1.13.9 fixed UserMenu mocks)
- Don't snapshot — assert specific text / role / aria
- Each test names the scenario clearly

## Candidates (pick top 3-5 by user impact)

Likely high-value:
- `frontend/src/routes/admin/clusters/$cid/buckets/$bid/index.tsx` (bucket browser; central UX)
- `frontend/src/routes/admin/system.tsx` SkinsManager (just landed in v1.13.1; needs coverage)
- `frontend/src/shared/auth/elevation.tsx` (elevation prompt; security-critical)
- `frontend/src/shared/auth/ProtectedRoute.tsx` (route guard)
- `frontend/src/shared/ui/UserMenu.tsx` (just fixed in v1.11.0.30; pin the fix)
- `frontend/src/routes/files/keys/index.tsx` (user-facing key management)

Pick whichever 3-5 have the highest user-impact + lowest current coverage.

## Hard constraints

- `pnpm build` + `go test -race ./...` green BEFORE push (run SYNCHRONOUSLY)
- `git commit <path>` pathspec form
- NO Co-Authored-By, NO `--no-verify`, NO emojis
- Per [[feedback_squash_commits]]: ONE commit
- Per [[feedback_freshman_must_merge_back_to_main]]: push HEAD:main FIRST, verify, then tag

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v1.13.16
git push origin v1.13.16
git ls-remote origin v1.13.16
```

## Acceptance

1. 3-5 frontend modules reach ≥80% coverage (commit lists per-module before/after)
2. New tests follow Testing Library patterns
3. Vitest stays green; no flake introduced
4. Tag + main same commit; both pushed

Return summary AFTER both pushes succeed.

Estimated complexity: 90-120 min.
