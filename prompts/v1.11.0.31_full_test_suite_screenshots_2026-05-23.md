You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui.

# Cycle: v1.11.0.31 — Full test suite re-run + screenshot pass

**Operator request**: "you need to run full testing suite against everything again. screenshots in playwright etc"

Since the v1.11.0.20→.30 + v1.12.0a/b + v1.13.0a/b chain shipped a LOT of FE + BE changes (login route, UserMenu mode-fix, NewVersionBanner heartbeat, CSK envelope encryption, skin system, Gateways UX polish, mutation-invalidation audit, etc.), comprehensive regression run is overdue.

## What to run

In order, all against the latest tag deployed to https://basement.pq.io (wait for Watchtower to roll v1.11.0.30 before starting):

1. **`pnpm build`** — verify the bundle builds clean
2. **`go test -race ./...`** — full backend test suite
3. **`pnpm test:run`** — full frontend vitest suite
4. **`bash scripts/postdeploy-ui-smoke.sh`** against live basement.pq.io with `matthew/password`
5. **`pnpm smoke:full`** (comprehensive-smoke.ts) — 127 checks across desktop + mobile viewports, ephemeral resource lifecycle. Use `garage-v2-test-*` clusters for any destructive ops per [[feedback_operator_data_off_limits]].
6. **`bash scripts/feature-smoke.sh`** — A-O feature coverage on test backends only
7. **`bash scripts/mobile-audit.sh`** (from v1.11.0.17) — re-audit mobile viewports; expect MAJOR count near zero now that the AppShell + Button + Input fixes shipped

## Screenshots

The comprehensive-smoke writes screenshots to `/tmp/basement-smoke-<timestamp>/`. After the run completes, **copy the latest snapshot set into `docs/screenshots/v1.11.0.30/`** (one new dir per re-test pass). Diff against `docs/screenshots/v1.10/` to spot any visual regressions in:
- /admin/clusters list
- /admin/clusters/{cid} detail
- /admin/system (with new skin UI section per v1.13.0b)
- /admin/keys (was removed in v1.11.0.15 — should 404 cleanly)
- /files bucket browser
- /login (post v1.11.0.24 route rename)
- UserMenu open in both user mode + admin mode (verify v1.11.0.30 fix)
- NewVersionBanner (force-trigger by mismatching X-Build header)

## Output

`docs/test-runs/2026-05-23_full_suite.md` — per-suite pass/fail counts, screenshot diff notes (which dirs changed, what's new, what regressed), any new bug repros for the next cycle.

## Issue triage

For EACH new bug found:
- Sev 1 (broken core flow): document in `docs/test-runs/2026-05-23_full_suite.md` BUG section, escalate to senior immediately (don't dispatch a fix in this cycle)
- Sev 2/3 (UX nit, edge case): document with file:line if known; queue for next bug-hunt cycle
- Visual regression: include the before/after screenshot pair

## Hard constraints

- READ-ONLY against operator's real data (no creates/deletes on classe, lsi, cheshire). Use garage-v2-test-* clusters for destructive ops.
- `pnpm build` + `go test -race ./...` MUST be green at the START (sanity check before the deeper runs)
- Per [[feedback_squash_commits]]: ONE commit covering the test run report + screenshot drops
- Per [[feedback_freshman_must_merge_back_to_main]]: push HEAD:main FIRST then tag
- Per [[feedback_prompts_in_repo]]: any new prompts you write live in basement-ui/prompts/, not /tmp

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v1.11.0.31
git push origin v1.11.0.31
git ls-remote origin v1.11.0.31
```

## Acceptance

1. All 7 suites (build/test-go/test-vitest/postdeploy-smoke/comprehensive/feature/mobile) RUN — even if some fail
2. `docs/test-runs/2026-05-23_full_suite.md` documents counts + new bugs
3. `docs/screenshots/v1.11.0.30/` populated with the comprehensive-smoke shot set
4. Visual diff against v1.10 noted
5. Tag + main same commit; both pushed
6. Sev 1 bugs escalated; Sev 2/3 queued

Return summary: counts + bug list + any escalations.

Estimated complexity: 2-3 hours (mostly waiting on suite runs).
