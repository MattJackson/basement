You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui.

# Cycle: v1.13.4 — Fix React error #321

The v1.13.3 cycle investigated React error #321 (invalid hook call) and wrote findings to `docs/react-error-321-repro.md` + a Playwright spec at `frontend/react-error-321.spec.ts`. Now FIX the root cause.

## Read first

- `docs/react-error-321-repro.md` — the investigation findings + suspected trigger points
- `frontend/react-error-321.spec.ts` — the Playwright spec the previous cycle wrote
- The suspected callsites identified in the doc (SkinsManager debounced save, query refetch chain)

## Investigation step

If the doc's investigation didn't pin a specific file:line root cause, do it now:
- Run `pnpm test --run` and grep for React error #321 in console
- Run `pnpm vitest --run frontend/react-error-321.spec.ts` (or whatever the spec runs against)
- React error #321 = "Invalid hook call. Hooks can only be called inside of the body of a function component." Common causes:
  - Hook called inside a conditional / loop / nested function (not at top of component)
  - Hook called outside React render tree (in event handler, async callback, plain function)
  - Two copies of React in the bundle (rare, check via pnpm why react)
  - Forgetting to wrap a hook call in a component (e.g. const x = useUser() at module scope)

## Fix

Once root cause is located:
- Move the hook call to component body / top of render
- OR refactor non-component caller to receive the data via props/context
- OR dedupe React copies if that's the cause
- Whatever the fix shape, keep it minimal

## Tests

- Update / extend `frontend/react-error-321.spec.ts` (or wherever the spec lives) to ASSERT zero React error #321 console errors across every route walked
- Existing vitest tests for affected components must stay green

## Hard constraints

- `pnpm build` + `go test -race ./...` green BEFORE push (run SYNCHRONOUSLY)
- `git commit <path>` pathspec form
- NO Co-Authored-By, NO `--no-verify`, NO emojis
- Per [[feedback_squash_commits]]: ONE commit
- Per [[feedback_freshman_must_merge_back_to_main]]: push HEAD:main FIRST, verify, then tag

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v1.13.4
git push origin v1.13.4
git ls-remote origin v1.13.4
```

## Acceptance

1. React error #321 root cause located (file:line in commit message)
2. Fix shipped + verified by the spec going green
3. No new React errors introduced
4. Tag + main same commit; both pushed

Return summary AFTER both pushes succeed. If the root cause is genuinely undiagnosable from the available evidence, escalate to senior with what you tried + what's still unknown — don't ship a speculative fix.

Estimated complexity: 45-75 min.
