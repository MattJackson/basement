# v2.0.0-beta.32 — i18n the cluster detail page

## What you're producing

ONE commit, ONE tag. Operator switched language to Spanish but
`/admin/clusters/{cid}` (the cluster detail page) is entirely in
English. i18n coverage gap.

## Investigation step (do this FIRST)

Don't guess. Open `frontend/src/routes/admin/clusters/$cid/index.tsx`
and scan for hard-coded English strings. Greppable patterns:

```bash
grep -nE '>[A-Z][a-z][a-zA-Z ]+<|placeholder=".+"|title=".+"|aria-label=".+"' \
  /Users/mjackson/Developer/basement-ui/frontend/src/routes/admin/clusters/\$cid/index.tsx \
  | head -40
```

Build a checklist of every English string in the file (column headers,
button labels, empty-state text, modal copy, etc).

## Then

For each English string:

1. Pick an i18n key. Use the project convention — see
   `frontend/src/shared/i18n/locales/en/pages.json` for the
   `clusterDetail.*` keys (or whatever namespace already exists for
   admin/clusters).
2. Wrap the string in `t("namespace.key")`.
3. Add the key to BOTH `en/pages.json` (English) AND `es/pages.json`
   (Spanish, translate as best you can — operator can refine later).
4. If `useTranslation` isn't already imported in the file, add it.

The file at line 536 has `← Back to Clusters` (already flagged for
beta.30). Don't touch it — beta.30 will conditionally hide it. Just
i18n the surrounding text.

## Verify live

After deploy, paste this output:

```bash
cat > /tmp/i18n_verify.mjs <<'JS'
import { chromium } from "/Users/mjackson/Developer/basement-ui/frontend/node_modules/playwright/index.mjs";
const BASE = "https://basement.pq.io";
const browser = await chromium.launch();
const ctx = await browser.newContext({ viewport: { width: 1280, height: 900 } });
const r = await fetch(`${BASE}/api/v1/auth/login`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ username: "matthew", password: "password" }) });
const m = (r.headers.get("set-cookie") || "").match(/(__Host-[^=]+)=([^;]+)/);
await ctx.addCookies([{ name: m[1], value: m[2], domain: "basement.pq.io", path: "/", httpOnly: true, secure: true, sameSite: "Strict" }]);
const page = await ctx.newPage();
await page.request.post(`${BASE}/api/v1/auth/elevate`, { data: { target_mode: "admin", password: "password" } });
await page.request.put(`${BASE}/api/v1/auth/active-role`, { data: { kind: "ui-admin" } });

// Discover a cluster id
const cl = await (await page.request.get(`${BASE}/api/v1/admin/clusters`)).json();
const cid = cl[0]?.id;

// Set language to Spanish
await page.addInitScript(() => localStorage.setItem("i18nextLng", "es"));
await page.goto(`${BASE}/admin/clusters/${cid}`, { waitUntil: "networkidle" });
await page.waitForTimeout(800);

// Grab visible text and sample a few specific things expected to be in Spanish
const headings = await page.$$eval("h1, h2, h3", els => els.map(e => e.textContent?.trim()));
console.log("headings:", headings);
const body = await page.locator("main").innerText().catch(() => "");
console.log("body snippet (first 500 chars):", body.slice(0, 500));
await browser.close();
JS
node /tmp/i18n_verify.mjs
```

Expected: headings + body show Spanish copy, not English.

## Anti-fabrication gates (NON-NEGOTIABLE)

- Every numeric / symbolic claim has a pasted-output citation
- Every existing-code reference cites `file:line`
- When the source doesn't say what you need, mark **OPEN** — DO NOT invent
- `git status --short` before commit
- `git -C <absolute-path>` always
- Stage by file name. NEVER `git add .` or `-A`
- NO `--no-verify` / `Co-authored-by` / `--force`
- ONE commit + ONE tag this cycle
- **Run `pnpm build` before pushing.**
- Translation quality: best-effort, operator will refine. DO NOT invent
  technical terms — when in doubt, leave the technical term in English
  (cluster, bucket, key, scrub, lifecycle) and just translate the
  surrounding copy.

## Process

1. `git -C /Users/mjackson/Developer/basement-ui pull --ff-only origin main`
2. Grep for English strings in the cluster detail file.
3. Wrap + add keys to en + es JSON files.
4. `pnpm tsc --noEmit && pnpm build && pnpm test:run`.
5. Stage explicit files. ONE commit: `v2.0.0-beta.32: i18n cluster detail page`.
6. Push HEAD:main, tag, push tag, ls-remote.
7. Wait for deploy. Verify.

## What success looks like

- 3-4 files edited: `routes/admin/clusters/$cid/index.tsx` +
  `locales/en/pages.json` + `locales/es/pages.json` (+ maybe the lifecycle/edit sub-routes if they share the same component tree)
- All checks green
- Verify shows Spanish text on the page after `i18nextLng=es`

## Working repo

`/Users/mjackson/Developer/basement-ui`

## Mandatory ordering at the bottom

```bash
git -C /Users/mjackson/Developer/basement-ui push origin HEAD:main
git -C /Users/mjackson/Developer/basement-ui push origin v2.0.0-beta.32
git -C /Users/mjackson/Developer/basement-ui ls-remote origin v2.0.0-beta.32
```
