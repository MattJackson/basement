You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui. **v2.0 MILESTONE WORK** — first of multiple v2.0.0a/b/c cycles. Operator has committed to a clean-break v2.0: no backward-compat shims, no deprecated code paths.

# Cycle: v2.0.0a — Drop the bucket_user role entirely

`bucket_user` was a v1.1.0 role that was deprecated in v1.4 (new assignments became no-ops). Per [[v2_clean_break]] the role itself goes now.

## Read first

- `internal/auth/policy/types.go` — the role definition
- `internal/auth/policy/enforcer.go` — where bucket_user is checked
- `internal/auth/policy/enforcer_test.go` — tests against bucket_user assertions
- `internal/api/admin_policy_sim.go` + `_test.go` — policy simulator
- `internal/api/policy_gates_test.go`, `admin_policies_test.go`, `admin_audit_test.go`, `admin_policy_sim_test.go` — all test files referencing the role
- `internal/api/user_shares.go` — runtime usage if any
- `frontend/src/routes/admin/policies.tsx` — UI dropdown / picker
- `docs/release-notes/v1.0.0.md` + `v1.1.0.md` + `docs/adr/0001-rbac-three-tier-creds.md` + `0002-region-tier-user-model.md` — historical record (UPDATE WITH NOTE about removal, do not erase)

## Tasks

1. **Type removal**: drop `bucket_user` from the `Role` enum / type. If the type was a union of string literals, remove the literal + bump the type
2. **Enforcer removal**: drop every branch that handles `bucket_user`. Replace with comment explaining v2.0 removal IF the surrounding code structure makes the removal non-obvious
3. **FE picker**: remove `bucket_user` from the Role dropdown in policies admin
4. **Tests**: remove tests for `bucket_user` behavior; if a test was asserting "bucket_user is a no-op" that test can go too
5. **Data migration**: any RoleAssignment with `role: "bucket_user"` on first v2.0.0 boot should be SILENTLY DROPPED from `store/policy_assignments.json` (or wherever they live). Write the migration as a one-shot in the store loader; log how many were removed at WARN level so operators see it in startup logs
6. **Docs**: add `docs/upgrading/v1-to-v2.md` (create file) — first entry: "Removed: `bucket_user` role. RoleAssignments with role=bucket_user are silently dropped on first v2.0 boot. There is no replacement; use bucket-level shares or per-region UserKeychain instead."
7. **CHANGELOG**: add a `## BREAKING` section to the v2.0.0a entry
8. **ADR-0001 + ADR-0002**: append a "Resolution (v2.0)" section noting bucket_user was removed; do not erase original content

## End-to-end user-flow trace (REQUIRED per [[feedback_verify_outcome_not_diff]])

Document in the commit message:
- A platform admin opens /admin/policies → no `bucket_user` option in the role dropdown
- A platform admin attempts to POST `/admin/policies/assignments` with `role: "bucket_user"` → server returns 400 "unknown role"
- Existing data with `role: "bucket_user"` in policy_assignments.json → on next basement startup, those rows are dropped + a WARN log line states the count
- A Playwright OR vitest test exists that drives the dropdown OR POSTs the request and asserts the absence

## Hard constraints

- `pnpm build` + `go test -race ./...` green BEFORE push (run SYNCHRONOUSLY)
- `git commit <path>` pathspec form
- NO Co-Authored-By, NO `--no-verify`, NO emojis
- Per [[feedback_squash_commits]]: ONE commit
- Per [[feedback_freshman_must_merge_back_to_main]]: push HEAD:main FIRST, verify, then tag
- Per [[v2_clean_break]]: NO new backward-compat code; the migration drops legacy data without preserving it
- Per [[feedback_verify_outcome_not_diff]]: commit message includes the user-flow trace above

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v2.0.0a
git push origin v2.0.0a
git ls-remote origin v2.0.0a
```

## Acceptance

1. `bucket_user` removed from the Role type + enforcer + tests + FE picker
2. One-shot migration drops legacy assignments + logs the count
3. `docs/upgrading/v1-to-v2.md` created with the bucket_user entry
4. CHANGELOG BREAKING section started
5. ADR-0001 + ADR-0002 appended (not rewritten)
6. End-to-end user-flow traced in commit message
7. Tag + main same commit; both pushed

Return summary AFTER both pushes succeed. Estimated complexity: 60-90 min.
