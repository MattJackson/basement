# v1.12.0c: CSK Lock-State Awareness Verification (RE-DISPATCH)

## Background
Worker wrote documentation but test file has compilation errors. Fix the tests now.

## Key Finding from Previous Attempt
Documentation at `docs/cycle-reports/v1.12.0c-csk-awareness-verification.md` is complete and correct.

Test file exists at `internal/api/backup_runner_test.go` but needs fixing:
- backup.Runner is an **interface type**, NOT a function
- To use it, create `backup.RunnerFunc` closure (see internal/backup/scheduler.go)
- Pattern: `backup.RunnerFunc(func(ctx context.Context, b backup.Backup) backup.BackupResult { ... })`

## Task Requirements

### 1. Fix the Test File
Read `internal/api/backup_runner_test.go` and fix all compilation errors:
- Remove duplicate code blocks (testMock declared twice in same function)
- Use proper Runner invocation pattern with backup.RunnerFunc closure
- Ensure driver registry is wired so tests can actually run

### 2. Run Tests
```bash
go test -race ./internal/api -run TestBackupRunner
```
All tests must PASS before proceeding.

### 3. Tag Release
Once tests pass:
```bash
git commit internal/api/backup_runner_test.go docs/cycle-reports/v1.12.0c-csk-awareness-verification.md
git tag v1.12.0c
git push origin main
git push origin v1.12.0c
```

## Constraints
- NO Co-Authored-By
- NO emojis  
- Use git commit <path> form
- Push HEAD:main FIRST, verify, then tag
