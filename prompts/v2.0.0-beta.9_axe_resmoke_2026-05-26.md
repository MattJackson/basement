# v2.0.0-beta.9 — install @axe-core/playwright + re-run comprehensive smoke

## What you're producing

1. Install `@axe-core/playwright` as a dev dependency.
2. Re-run `bash scripts/comprehensive-smoke.sh` against the live deploy
   to verify the v2.0.0-beta.5/6/7/8 fixes actually work end-to-end
   AND capture the a11y audit that was previously skipped.
3. Save the smoke log + screenshot path to `/tmp/basement/`.
4. Write a brief findings.md summarizing pass/fail counts, console
   errors, and any a11y violations.
5. Commit + tag.

## Background

Comprehensive smoke previously logged
`[a11y] @axe-core/playwright not installed — a11y audit skipped`.
The wiring at `scripts/comprehensive-smoke.ts` lines 80-110 + ~1406 is
ready; we just need the package installed.

Recent fixes that the smoke should now confirm:
- B5 login subtitle ("Username Password" text gone)
- B6 signupMode renders correctly even when persisted as ""
- B7 smoke harness now sets activeRole=ui-admin → admin routes
  shouldn't bounce, ephemeral SA create should pass
- B8 /admin/login uses TanStack redirect (no console error)
- B2 UI Admin can reach /admin/clusters/new (smoke walks this route)
- B3 /admin/service-accounts default view hides revoked (smoke doesn't
  test this but it's visible in screenshots)

## Read these first (absolute paths, with why)

- `/Users/mjackson/Developer/basement-ui/scripts/comprehensive-smoke.ts`
  — lines 80-110 (axe-core dynamic import); ~1406 (a11y rollup section).
  Don't edit; just confirm the wiring is intact.

- `/Users/mjackson/Developer/basement-ui/frontend/package.json`
  — devDependencies section. After install, the new entry should
  appear alphabetically. Lockfile changes are expected.

- `/Users/mjackson/Developer/basement-ui/CHANGELOG.md` (if exists) —
  add a v2.0.0-beta.9 entry.

## Exact steps

1. `git -C /Users/mjackson/Developer/basement-ui pull --ff-only origin main`
2. Confirm live version: `curl -s https://basement.pq.io/api/v1/version`.
   If it's not at least v2.0.0-beta.6, **ESCALATE** — the smoke needs the
   B2/B4 fixes deployed to validate them. Don't proceed.
3. `cd /Users/mjackson/Developer/basement-ui/frontend && pnpm add -D @axe-core/playwright`
4. `cd /Users/mjackson/Developer/basement-ui/frontend && pnpm tsc --noEmit` — must pass.
5. `cd /Users/mjackson/Developer/basement-ui/frontend && pnpm build` — must pass.
6. Run the smoke:
   `mkdir -p /tmp/basement && bash /Users/mjackson/Developer/basement-ui/scripts/comprehensive-smoke.sh > /tmp/basement/smoke_beta9_$(date +%Y%m%d_%H%M%S).log 2>&1; echo "exit=$?"`
7. Read the log. Note: pass count, fail count, console error count,
   a11y violation count (look for `[a11y]` lines), screenshot dir.
8. Write `/tmp/basement/smoke_beta9_findings.md` with:
   - Pass / fail / skip counts
   - Each failed check name + the error
   - Any console errors (top 5)
   - Any a11y violations (top 10, grouped by rule)
   - Screenshot dir path
   - Verdict: was B2/B5/B6/B7/B8 confirmed working live?
9. Stage + commit ONLY the package.json + pnpm-lock.yaml changes (NOT
   the smoke output, that's local-only):
   `git -C /Users/mjackson/Developer/basement-ui add frontend/package.json frontend/pnpm-lock.yaml`
10. `git -C /Users/mjackson/Developer/basement-ui commit -m "v2.0.0-beta.9: install @axe-core/playwright — unblocks a11y audit in comprehensive-smoke"`
11. `git -C /Users/mjackson/Developer/basement-ui push origin HEAD:main`
12. `git -C /Users/mjackson/Developer/basement-ui tag v2.0.0-beta.9`
13. `git -C /Users/mjackson/Developer/basement-ui push origin v2.0.0-beta.9`
14. `git ls-remote origin v2.0.0-beta.9`

## Anti-fabrication gates (NON-NEGOTIABLE)

- Every numeric / symbolic claim has a pasted-output citation
- Every existing-code reference cites `file:line`
- When the source doesn't say what you need, mark **OPEN** — DO NOT invent
- Don't reinterpret a prompt to fit available evidence; flag the gap
- `git status --short` before commit
- `git -C <absolute-path>` always; NEVER `cd <path> && git ...`
- Stage by file name: `git add path/to/file1 path/to/file2`. NEVER `git add .` or `-A`
- NO `--no-verify` on commits (even when no hook is installed)
- NO `Co-authored-by` text anywhere in the commit message
- DO NOT exit before pushing — if you can't push, return WITH a clear escalation reason

## What success looks like

- @axe-core/playwright installed (visible in package.json devDependencies)
- pnpm-lock.yaml updated
- tsc + build green
- Comprehensive smoke ran to completion (exit 0 OR exit 1 with a clear
  list of which checks failed; either is OK — we want to know)
- findings.md written with concrete counts
- commit + tag landed on main, tag reachable
- One DONE summary at end including: pass/fail/console-errors/a11y counts +
  smoke log path + screenshot dir + commit sha

## What failure looks like

- npm install conflicts (resolve in scope or **ESCALATE**)
- Smoke can't reach the live deploy (**ESCALATE** — likely network)
- Live deploy is still on pre-beta.6 (per step 2, **ESCALATE** — wait for
  Watchtower to roll)
- tsc/build break after install (**ESCALATE**)

## Working repo

`/Users/mjackson/Developer/basement-ui`

## Mandatory ordering at the bottom

```bash
git -C /Users/mjackson/Developer/basement-ui push origin HEAD:main       # MUST land first
git -C /Users/mjackson/Developer/basement-ui push origin v2.0.0-beta.9   # only after main
git ls-remote origin v2.0.0-beta.9                                       # confirm tag at main
```
