You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui.

# Cycle: v1.13.9 — Fix the 40 pre-existing vitest failures + verify postdeploy-ui-smoke recovered

The v1.13.8 triage cleared the React #321 regression. Two follow-on tasks:

1. **Verify postdeploy-ui-smoke recovered**: it was 9/30/10 before v1.13.8. Should be back near 81/3 (the morning baseline) now that React #321 is gone (most of those failures were React errors). Run + capture.

2. **Fix the 40 pre-existing vitest failures**: v1.13.8 narrowed them down to "pre-existing mock issues". Go through each + fix. Common patterns:
   - Missing mocks for `@/shared/api/queries` hooks → add to setup file
   - Type mismatches after recent type-narrowing in queries.ts
   - Removed exports referenced by old tests (e.g. `useEnableWebDAV`, `useDismissOnboarding`)
   - Snapshot test stale (regenerate with `pnpm vitest -u`)

## Read first

- `frontend/vitest.config.ts`
- `frontend/src/test/setup.ts` (or wherever global mocks live)
- Run `pnpm vitest --run 2>&1 | tail -100` to get the actual failure list

## Approach

1. Run `pnpm vitest --run` + capture the failure list grouped by file
2. For each failing test, classify:
   - **mock issue**: missing mock for a hook → add to setup or per-test
   - **type issue**: TS error → fix the type or update the test
   - **stale snapshot**: regenerate only if visually intentional (operator wouldn't notice)
   - **real bug**: rare here, but flag if you find one — escalate
3. Fix in batches by failure type
4. Re-run `pnpm vitest --run` until 0 failing
5. Run `pnpm vitest --coverage --run` to get a coverage delta for the report

## Hard constraints

- `pnpm build` + `go test -race ./...` green BEFORE push (run SYNCHRONOUSLY)
- `git commit <path>` pathspec form
- NO Co-Authored-By, NO `--no-verify`, NO emojis
- Per [[feedback_squash_commits]]: ONE commit
- Per [[feedback_freshman_must_merge_back_to_main]]: push HEAD:main FIRST, verify, then tag
- Do NOT delete tests that fail — fix them. Only remove a test if it tests a feature that no longer exists (per the v1.11.0.15 keys route removal pattern); document that in the commit

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v1.13.9
git push origin v1.13.9
git ls-remote origin v1.13.9
```

## Acceptance

1. `pnpm vitest --run` → 0 failures
2. Coverage delta captured in commit message (before/after)
3. postdeploy-ui-smoke recovery verified (run + record pass/fail counts)
4. Tag + main same commit; both pushed

Return summary AFTER both pushes succeed.

Estimated complexity: 60-120 min depending on mock complexity.
