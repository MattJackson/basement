You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui.

# Cycle: v1.13.15 — Go test-coverage backfill round 2

v1.13.13 took 3 packages to ≥80%. Continue. Read `docs/coverage-backlog-2026-05-24.md` for the prioritized list + pick the NEXT 3-4 packages by impact.

## Approach

For each target package:

1. `go test -cover -coverprofile=/tmp/<pkg>.out ./internal/<pkg>/...` to capture current coverage
2. `go tool cover -func=/tmp/<pkg>.out | awk '$3+0 < 80 { print }'` to see uncovered functions
3. Write unit tests covering:
   - Uncovered error paths first (highest bug risk)
   - Boundary conditions next (off-by-one, nil, empty, max)
   - Happy-path branches last (lowest bug risk)
4. Re-run `go test -cover` to confirm ≥80% per package
5. Move to next package

## Quality bar

- New tests must NOT shell out, NOT require network, NOT require Docker — pure-Go unit tests only
- Mock external deps using interfaces already present in the package; don't introduce new mocking frameworks
- Each test name documents the scenario being exercised (table-driven style preferred)
- Tests should fail informatively (e.g. `t.Errorf("Foo(\"%s\") = %d, want %d", input, got, want)`)
- Skip tests with `t.Skip("requires integration env")` if the function genuinely needs Docker/Postgres/etc — flag for integration cycle

## Candidates (pick top 3-4 by impact)

From the backlog, likely highest-value:
- `internal/api/admin_users.go` (auth + user lifecycle — high blast radius if broken)
- `internal/api/admin_webhooks.go` (webhook delivery — operator surface)
- `internal/store/connections.go` (cluster connection persistence — load-bearing)
- `internal/federation/engine.go` (replication logic — already has decent tests; check what's missing)
- `internal/serviceaccount/serviceaccount.go` (bearer auth — security-critical)

Pick whichever 3-4 have the highest current coverage gaps + are most user-visible. Skip generated code + main.go.

## Hard constraints

- `pnpm build` + `go test -race ./...` green BEFORE push (run SYNCHRONOUSLY)
- `git commit <path>` pathspec form
- NO Co-Authored-By, NO `--no-verify`, NO emojis
- Per [[feedback_squash_commits]]: ONE commit
- Per [[feedback_freshman_must_merge_back_to_main]]: push HEAD:main FIRST, verify, then tag

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v1.13.15
git push origin v1.13.15
git ls-remote origin v1.13.15
```

## Acceptance

1. 3-4 Go packages reach ≥80% coverage on the target functions
2. Commit message lists each: package X function Y was N%, now M%
3. All existing tests stay green (no regressions)
4. Tag + main same commit; both pushed

Return summary AFTER both pushes succeed.

Estimated complexity: 90-150 min.
