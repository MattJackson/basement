# v2.0.0-beta.12 — a11y detailed audit + top color-contrast fixes

## What you're producing

1. A one-off Playwright script `scripts/a11y-detail-dump.ts` that walks
   the routes flagged with color-contrast violations and dumps the
   FAILING ELEMENTS (selector, HTML snippet, foreground/background color,
   contrast ratio) in JSON.
2. Run it. Analyze the top 5 most common failing element/color pairs.
3. Fix the TOP 3 most-frequent patterns (one CSS-token or component
   change each).
4. Re-run comprehensive smoke and compare beta.10 baseline (113
   violations / 132 color-contrast occurrences) to beta.12 numbers.
5. Commit + tag + push.

## Background

beta.10 darkened `--color-muted-foreground` but axe-core color-contrast
count stayed at 132. The violations are on OTHER tokens or inline styles.
Without specific failing elements we're guessing — this cycle gets the
ground truth.

## Read these first

- `/Users/mjackson/Developer/basement-ui/scripts/comprehensive-smoke.ts`
  lines 80-110 (axe dynamic import pattern), 212-226 (runAxe), 242-268
  (loginViaApi), 270-285 (elevateToAdmin). USE these patterns; don't
  reinvent.

- `/Users/mjackson/Developer/basement-ui/frontend/src/index.css`
  — all theme tokens are here; understand the full palette before
  touching.

- `/tmp/basement/smoke_beta10_findings.md` — the freshman's prior
  findings; corroborate against your new detail dump.

## Implementation — `scripts/a11y-detail-dump.ts`

A self-contained script (similar shape to comprehensive-smoke.ts but
single-purpose). Outline:

```ts
#!/usr/bin/env node
// scripts/a11y-detail-dump.ts — dumps detailed axe-core violations
// (per element, with color info) for a focused subset of routes,
// helping triage which specific elements/tokens fail color-contrast.
//
// Output: /tmp/basement/a11y-detail-{ts}.json with full per-element
// violation data.

import type { Page, chromium as ChromiumApi } from "playwright";
import { existsSync, writeFileSync, mkdirSync } from "node:fs";
import { join, resolve, dirname } from "node:path";
import { fileURLToPath, pathToFileURL } from "node:url";

const __dirname = dirname(fileURLToPath(import.meta.url));
const FRONTEND_NM = resolve(__dirname, "..", "frontend", "node_modules");
const PLAYWRIGHT = join(FRONTEND_NM, "playwright", "index.mjs");
const AXE = join(FRONTEND_NM, "@axe-core", "playwright", "dist", "index.js");

const { chromium } = (await import(pathToFileURL(PLAYWRIGHT).href)) as { chromium: typeof ChromiumApi };
const { default: AxeBuilder } = (await import(pathToFileURL(AXE).href)) as { default: any };

const BASE_URL = process.env.BASE_URL ?? "https://basement.pq.io";
const USERNAME = process.env.BUI_USERNAME ?? "matthew";
const PASSWORD = process.env.BUI_PASSWORD ?? "password";

const ROUTES = [
  "/", "/login", "/admin/login",
  "/files", "/files/keys", "/files/shares", "/files/syncs",
  "/files/backups", "/files/backups/new", "/files/federated-buckets",
  "/files/webhooks", "/files/webhooks/new",
  "/admin/system", "/admin/users", "/admin/clusters", "/admin/audit",
  "/admin/service-accounts", "/admin/policies", "/admin/migrate",
  "/admin/usage",
];

async function loginAndSetRole(ctx: any) {
  // POST /api/v1/auth/login, inject cookie (copy from comprehensive-smoke.ts:242-267)
  // Then use page.request for elevate + PUT /auth/active-role kind:ui-admin
}

(async () => {
  const browser = await chromium.launch();
  const ctx = await browser.newContext({ viewport: { width: 1280, height: 900 } });
  // ... login + elevate + active-role ...
  const page = await ctx.newPage();
  const allViolations: any[] = [];
  for (const route of ROUTES) {
    try {
      await page.goto(`${BASE_URL}${route}`, { waitUntil: "networkidle", timeout: 15_000 });
      const audit = await new AxeBuilder({ page }).analyze();
      for (const v of audit.violations) {
        for (const node of v.nodes) {
          allViolations.push({
            route,
            rule: v.id,
            impact: v.impact,
            target: node.target,
            html: node.html.slice(0, 300),
            failureSummary: node.failureSummary,
            // axe puts color info in `any` array on color-contrast violations
            colorInfo: (node as any).any?.find((a: any) => a.id === "color-contrast")?.data ?? null,
          });
        }
      }
    } catch (e) {
      console.error(`route ${route}:`, e);
    }
  }
  const outPath = `/tmp/basement/a11y-detail-${Date.now()}.json`;
  mkdirSync("/tmp/basement", { recursive: true });
  writeFileSync(outPath, JSON.stringify(allViolations, null, 2));
  console.log(`wrote ${allViolations.length} violation entries to ${outPath}`);
  await browser.close();
})();
```

Run with: `cd frontend && node ../scripts/a11y-detail-dump.ts`.

## Analysis step

After the dump:

1. Read `/tmp/basement/a11y-detail-*.json` (newest one).
2. Filter to `rule == "color-contrast"`.
3. Group by `colorInfo.fgColor` + `colorInfo.bgColor` pair OR by CSS
   class/selector if colors are inline.
4. List the TOP 5 most frequent pairs (count, ratio, sample HTML).
5. Identify the responsible CSS token or component for each.

## Fix the top 3

For the 3 most frequent failing patterns:
- If it's a theme token (e.g. `--color-foreground` on `--color-card`),
  darken/lighten the relevant token in `frontend/src/index.css` until
  the ratio passes 4.5:1.
- If it's a component-level inline style (e.g. `text-muted-foreground/60`),
  change the opacity multiplier or class.
- If it's hardcoded color in a component file, replace with a token.

Conservative: don't change more than 3-4 token lines + ~3 component
files. Quality over coverage.

## Process checkpoints

1. `git -C /Users/mjackson/Developer/basement-ui pull --ff-only origin main`
2. Write `scripts/a11y-detail-dump.ts` per the outline above.
3. Run it: `cd /Users/mjackson/Developer/basement-ui/frontend && node ../scripts/a11y-detail-dump.ts 2>&1 | tail -10`
4. Read the JSON output; analyze; pick top 3 fixes.
5. Make the fixes (CSS token tweaks + maybe 1-2 component fixes).
6. `pnpm tsc --noEmit && pnpm build && pnpm test:run` — must pass.
7. Verify live deploy has reached beta.11 (poll `curl -s https://basement.pq.io/api/v1/version`).
   If not, **ESCALATE** — re-running smoke against beta.10 wouldn't validate beta.12 fixes.
8. Stage explicit files (the new dump script + the fixes):
   `git -C /Users/mjackson/Developer/basement-ui add scripts/a11y-detail-dump.ts frontend/src/index.css <other touched files>`
9. `git -C /Users/mjackson/Developer/basement-ui commit -m "v2.0.0-beta.12: a11y detail dumper + top-3 color-contrast fixes"`
10. `git -C /Users/mjackson/Developer/basement-ui push origin HEAD:main` — MUST land first
11. `git -C /Users/mjackson/Developer/basement-ui tag v2.0.0-beta.12`
12. `git -C /Users/mjackson/Developer/basement-ui push origin v2.0.0-beta.12`
13. `git ls-remote origin v2.0.0-beta.12`

14. **Critical pre-step before smoke re-run**: the prod cluster gets
    auto-re-seeded with driver=garage on every backend restart, so
    after the beta.12 deploy lands, the cluster will be Unavailable
    again. Re-PATCH it before running smoke:

    ```bash
    # auth setup
    curl -s -X POST -H 'Content-Type: application/json' -d '{"username":"matthew","password":"password"}' \
      https://basement.pq.io/api/v1/auth/login -c /tmp/basement/cookies.txt -o /dev/null
    curl -s -X POST -b /tmp/basement/cookies.txt -c /tmp/basement/cookies.txt -H 'Content-Type: application/json' \
      -d '{"target_mode":"admin","password":"password"}' https://basement.pq.io/api/v1/auth/elevate -o /dev/null
    curl -s -X PUT -b /tmp/basement/cookies.txt -c /tmp/basement/cookies.txt -H 'Content-Type: application/json' \
      -d '{"kind":"ui-admin"}' https://basement.pq.io/api/v1/auth/active-role -o /dev/null

    # check driver and PATCH if needed
    CID=$(curl -s -b /tmp/basement/cookies.txt https://basement.pq.io/api/v1/admin/clusters | python3 -c 'import json,sys; d=json.load(sys.stdin); print(d[0]["id"] if d else "")')
    DRIVER=$(curl -s -b /tmp/basement/cookies.txt https://basement.pq.io/api/v1/admin/clusters | python3 -c 'import json,sys; d=json.load(sys.stdin); print(d[0]["driver"] if d else "")')
    echo "cluster $CID driver=$DRIVER"
    if [[ "$DRIVER" == "garage" ]]; then
      curl -s -X PATCH -b /tmp/basement/cookies.txt -H 'Content-Type: application/json' \
        -d '{"driver":"garage-v1"}' \
        "https://basement.pq.io/api/v1/admin/clusters/$CID" -o /dev/null -w "patch_http=%{http_code}\n"
    fi
    ```

15. Wait for live deploy to reach v2.0.0-beta.12 (poll `curl -s https://basement.pq.io/api/v1/version` up to 10 min).

16. Re-run smoke:
    `mkdir -p /tmp/basement && bash /Users/mjackson/Developer/basement-ui/scripts/comprehensive-smoke.sh > /tmp/basement/smoke_beta12_$(date +%Y%m%d_%H%M%S).log 2>&1; echo "exit=$?"`

17. Write `/tmp/basement/smoke_beta12_findings.md` comparing beta.10
    baseline (113 violations / 132 color-contrast) vs beta.12.

## Anti-fabrication gates (NON-NEGOTIABLE)

- Every numeric / symbolic claim has a pasted-output citation
- Every existing-code reference cites `file:line`
- When the source doesn't say what you need, mark **OPEN** — DO NOT invent
- `git status --short` before commit
- `git -C <absolute-path>` always
- Stage by file name. NEVER `git add .` or `-A`
- NO `--no-verify` / `Co-authored-by`
- DO NOT exit before pushing — escalate if blocked

## What success looks like

- a11y-detail-dump.ts in the repo
- /tmp/basement/a11y-detail-*.json with concrete violation data
- 3-5 file changes for the top fixes
- Smoke re-run shows color-contrast count drop (target: < 100; was 132)
- DONE summary with: commit sha, before/after numbers, top 3 fixes
  applied, log path

## What failure looks like

- Detail dump errors → fix in scope or ESCALATE
- Top 3 patterns can't be fixed without large refactor → ship the
  smaller fixes you CAN do; document the harder ones in findings.md
- Smoke breaks → ESCALATE

## Working repo

`/Users/mjackson/Developer/basement-ui`

## Mandatory ordering at the bottom

```bash
git -C /Users/mjackson/Developer/basement-ui push origin HEAD:main
git -C /Users/mjackson/Developer/basement-ui push origin v2.0.0-beta.12
git ls-remote origin v2.0.0-beta.12
```
