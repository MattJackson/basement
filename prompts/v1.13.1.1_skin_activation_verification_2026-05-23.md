You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui.

# Cycle: v1.13.1.1 — Live verify skin activation end-to-end

v1.13.1 just shipped. Operator needs LIVE verification that skin activation actually re-skims the page on basement.pq.io after Watchtower rolls this version.

## Task

1. SSH into garage-v2-test-0 (or whichever test cluster has v1.13.1 deployed)
2. Navigate to /admin/system → Skins section
3. Change "Default Skin" dropdown to a different skin (e.g., basement-95 if current is basement-default)
4. Wait for PATCH to complete + "Saved" toast
5. **Verify the page actually re-renders with the new skin applied** — check:
   - CSS variables changed (--bg-primary, --text-primary, etc.)
   - Visual appearance matches selected skin's palette
   - No manual refresh needed (live update)

6. If it DOESN'T re-skin live:
   - Check React Query invalidation for `["org-capabilities"]` and `["skin", "active"]` in SkinsManager
   - Verify the hooks are actually triggering a refetch
   - Fix the activation flow so page updates automatically

7. If it DOES work, document the SHA + timestamp of successful verification

## Constraints

- Use garage-v2-test-* clusters only (per [[feedback_operator_data_off_limits]])
- `pnpm build` + `go test -race ./...` green BEFORE push
- `git commit <path>` pathspec form
- NO Co-Authored-By, NO `--no-verify`, NO emojis
- ONE commit per cycle
- Push HEAD:main FIRST, verify, then tag v1.13.1.1

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v1.13.1.1
git push origin v1.13.1.1
git ls-remote origin v1.13.1.1
```

If no changes needed (skin activation works), still commit a docs-only update to prompts/bug_hunt_log_2026-05-23.md with verification results, then push + tag.

## Acceptance

- If broken: fix + ship at v1.13.1.1
- If working: document success in bug_hunt_log + tag as verification complete

Return summary AFTER both pushes succeed.

Estimated complexity: 30 minutes (verification) or 2 hours (if hotfix needed).
