# v1.13.0d: Skins Release + Screenshots + Milestone Tag

## Background
v1.13.0a/b/c implemented the skin system (types, upload handler, FE rendering). This cycle releases it with documentation and screenshots.

## Task Requirements

### 1. Write Release Notes for v1.13.0
Create `docs/release-notes/v1.13.0.md` documenting:
- **v1.13.0a**: Skin types + registry foundation, basement-default skin
- **v1.13.0b**: Upload handler for custom skins (per-user admin upload)
- **v1.13.0c**: FE rendering of typography, density, borderRadius, footer, loginHero

Format matching existing release notes (see `docs/release-notes/v1.12.0.md`).

### 2. Capture Screenshots
Run screenshot pass for skins feature:

**Shot list to capture:**
- `/admin/system` — Skins management page showing registry dropdown and basement-default skin preview
- `/login` with custom skin configured (showing loginHero if set)
- User settings with skin selector visible (when policy permits user override)

Use Playwright from `scripts/capture-screenshots.ts` or existing pattern. Save to `docs/screenshots/v1.13/`. Update `README.md` in screenshots directory if needed.

### 3. Tag Milestone
After docs and screenshots:
```bash
git tag v1.13.0
git push origin main
git push origin v1.13.0
```

## Verification Before Push
- `pnpm build` MUST be green
- `go test -race ./...` MUST be green (unchanged from previous cycles)
- ONE commit only (squash all changes)
- Push HEAD:main FIRST, verify, then tag

## Anti-fabrication gates (NON-NEGOTIABLE)
- Every numeric / symbolic claim has a pasted-output citation
- Every existing-code reference cites `file:line`
- When the source doesn't say what you need, mark **OPEN** — DO NOT invent
- Don't reinterpret a prompt to fit available evidence; flag the gap
- `git status --short` before commit
- `git -C <absolute-path>` always; NEVER `cd <path> && git ...`
- Stage by file name: `git add path/to/file1 path/to/file2`. NEVER `git add .` or `-A`
- NO `--no-verify` on commits (even when no hook is installed)
- NO `Co-authored-by` text anywhere in the commit message
- DO NOT exit before pushing — if you can't push, return WITH a clear escalation reason

## Working repo
/Users/mjackson/Developer/basement-ui
