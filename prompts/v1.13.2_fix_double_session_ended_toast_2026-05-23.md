You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui.

# Cycle: v1.13.2 — Fix double "Admin session ended" toast on Switch to user view

Operator reports: clicking "Switch to user view" in UserMenu fires TWO identical "Admin session ended" toasts simultaneously.

## Root Cause Hypothesis

Both the UserMenu's drop-privileges handler AND a session-state-listener are firing the same toast. Need to dedupe — one toast source only.

## Investigation + Fix Plan

1. **Read** `frontend/src/shared/ui/UserMenu.tsx`
   - Find `handleSwitchToUser` function
   - Identify where "Admin session ended" toast is fired
   - Check if there's a session-state-listener elsewhere also firing this toast

2. **Search** for all occurrences of "Admin session ended" in the codebase:
   ```bash
   grep -rn "Admin session ended" frontend/src/
   ```

3. **Dedupe strategy**: Pick ONE source of truth for the toast
   - Option A: Only fire from `handleSwitchToUser`, remove any listener-based toasts
   - Option B: Use a centralized event/emitter pattern with debouncing
   - Recommended: Option A — keep it simple, one handler = one toast

4. **Verify** no other session-state listeners exist that might fire duplicate toasts

5. **Write test**: vitest unit test for UserMenu's `handleSwitchToUser` confirming single toast call

## Files likely involved

- `frontend/src/shared/ui/UserMenu.tsx` — main suspect
- `frontend/src/shared/auth/` — session state management (check for listeners)
- Any toast utility or hook being used

## Constraints

- `pnpm build` + `go test -race ./...` green BEFORE push
- `git commit <path>` pathspec form
- NO Co-Authored-By, NO `--no-verify`, NO emojis
- ONE commit per cycle
- Push HEAD:main FIRST, verify, then tag v1.13.2

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v1.13.2
git push origin v1.13.2
git ls-remote origin v1.13.2
```

## Acceptance

1. Single "Admin session ended" toast fires when switching to user view
2. vitest confirms single toast call in handleSwitchToUser
3. No regression on other session-related UX

Return summary AFTER both pushes succeed.

Estimated complexity: 1-2 hours (investigation + fix).
