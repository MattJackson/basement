You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui.

# Cycle: v1.13.5 — Mobile tap-target MINOR sweep

The overnight mobile-audit flagged 217 MINOR tap-target warnings — UI elements below the WCAG 2.5.5 / iOS HIG 44x44 floor. Sweep them.

## Read first

- `scripts/mobile-audit.ts` — the audit script that flags these
- Run `bash scripts/mobile-audit.sh` (or `pnpm tsx scripts/mobile-audit.ts`) to get the current list with file:line context if it produces one
- `docs/mobile-audit-*.md` — the most recent audit report (look at the MINOR section)
- v1.11.0.17 cycle's `AppShell.tsx` / `Button.tsx` / `Input.tsx` fixes — same pattern, applied at component level

## Approach

The right fix for MOST cases is component-level (not site-level): bump the base size of a frequently-used component, get hundreds of fixes for free.

1. **Inventory the offenders** — produce a list grouped by component (e.g. "DropdownMenuItem: 42 instances", "table-row-action button: 18 instances")
2. **For each component with >5 instances**: bump the component's base styles to satisfy the 44x44 floor. Pattern from v1.11.0.17:
   ```css
   min-h-[44px] min-w-[44px] sm:min-h-0 sm:min-w-0
   ```
   (44x44 on touch viewports; clears the floor on desktop where mouse precision is fine)
3. **For one-off instances** (1-2 occurrences): fix at the callsite
4. **Update the test**: existing mobile-audit MUST produce zero MAJOR + significantly fewer MINOR after this sweep

## Don't touch

- Components where 44x44 would break layout intentionally (e.g. inline status dots, tiny icon decorations that aren't interactive — those aren't tap targets anyway; verify they don't have onClick handlers before sizing them up)
- Component primitives that are already 44x44 on touch (the v1.11.0.17 trio: AppShell nav links, Button default/lg/icon/icon-lg, Input)

## Tests

- Re-run `bash scripts/mobile-audit.sh` after the sweep
- Document the before/after MINOR count in the commit message
- Existing vitest tests must stay green (look for snapshot tests on touched components; update snapshots only if visually intentional)

## Hard constraints

- `pnpm build` + `go test -race ./...` green BEFORE push (run SYNCHRONOUSLY)
- `git commit <path>` pathspec form
- NO Co-Authored-By, NO `--no-verify`, NO emojis
- Per [[feedback_squash_commits]]: ONE commit (this is a broad sweep but it's one coherent change)
- Per [[feedback_freshman_must_merge_back_to_main]]: push HEAD:main FIRST, verify, then tag

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v1.13.5
git push origin v1.13.5
git ls-remote origin v1.13.5
```

## Acceptance

1. Mobile-audit MINOR count dropped by ≥80% (from 217 baseline → ≤45)
2. No new MAJOR introduced
3. Snapshot tests updated only where visually intentional
4. Tag + main same commit; both pushed
5. Commit message documents before/after MINOR counts

Return summary AFTER both pushes succeed.

Estimated complexity: 60-90 min (mostly systematic).
