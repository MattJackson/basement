# v2.0.0-beta.17 — final smoke + CHANGELOG + rc.1 readiness check

## What you're producing

This is the rc.1-readiness cycle. After beta.16 lands its a11y fixes:

1. Run the comprehensive smoke + capture final numbers.
2. Update `CHANGELOG.md` with a v2.0.0 entry summarizing all the
   beta.5 → beta.16 changes.
3. Write `/tmp/basement/rc1_readiness.md` with a go/no-go decision
   against the rc.1 criteria.
4. If GO: tag v2.0.0-rc.1 (NOT v2.0.0 yet — that's later).
5. If NO-GO: file findings and stop. The senior will decide whether
   to dispatch another fix cycle or override.

## rc.1 criteria (all must pass)

| Criterion | Threshold |
|---|---|
| Smoke pass | ≥ 105 |
| Smoke fail | == 0 |
| Console errors | == 0 |
| A11y violations | ≤ 20 (or 100% concentrated on documented-as-deferred patterns) |
| Frontend tests | 449+ passing |
| Go unit tests | all packages green |
| Cluster auto-detection | works (driver=garage-v1 without manual PATCH) |

## Read these first

- `/tmp/basement/smoke_beta16_findings.md` — if it exists, that's the
  prior cycle's verdict. Build on it.

- `CHANGELOG.md` at repo root (if exists) — for the existing entry
  shape. If absent, look at recent commit messages on main for
  semantic style.

## Process checkpoints

1. `git -C /Users/mjackson/Developer/basement-ui pull --ff-only origin main`
2. Verify live is at v2.0.0-beta.16 (poll `curl -s https://basement.pq.io/api/v1/version`).
3. Re-run comprehensive smoke:
   `mkdir -p /tmp/basement && bash /Users/mjackson/Developer/basement-ui/scripts/comprehensive-smoke.sh > /tmp/basement/smoke_beta17_$(date +%Y%m%d_%H%M%S).log 2>&1; echo "exit=$?"`
4. Read the smoke log. Extract pass / fail / console / a11y counts.
5. Also run quick checks:
   - `cd /Users/mjackson/Developer/basement-ui/frontend && pnpm test:run 2>&1 | tail -3`
   - `go test -race ./... 2>&1 | tail -5`
   - Cluster sanity (from `/tmp/basement/cookies.txt` if still valid,
     otherwise re-login):
     ```
     curl -s -b /tmp/basement/cookies.txt https://basement.pq.io/api/v1/admin/clusters | python3 -c "import json,sys;d=json.load(sys.stdin);print(f'driver={d[0][\"driver\"]}' if d else 'no cluster')"
     ```
6. Update CHANGELOG.md. Add this entry (adapt header to your project's
   convention):

   ```markdown
   ## v2.0.0-rc.1 — 2026-05-27

   Release candidate for v2.0.0. Bundles beta.5 → beta.16 work.

   ### Fixed
   - Login subtitle no longer renders "Username Password" (v2.0.0-beta.5)
   - signupMode form fallback handles empty string (v2.0.0-beta.5)
   - /admin/login redirect uses TanStack `redirect()` cleanly (v2.0.0-beta.5)
   - Smoke harness now sets activeRole=ui-admin after every elevate, catches feat-smoke-* reap (v2.0.0-beta.5/.11)
   - UI Admin can reach /admin/clusters/new + per-cluster routes (v2.0.0-beta.6)
   - Role-gated /admin/* bounces show a toast instead of silent redirect (v2.0.0-beta.6)
   - OpenAPI spec structural bug at /admin/buckets (v2.0.0-beta.7)
   - /admin/service-accounts hides revoked SAs by default with a toggle (v2.0.0-beta.8)
   - Backend auto-detects Garage v1 vs v2 when seeding the default cluster (v2.0.0-beta.15)
   - A11y violations across the app dropped by ~80% (v2.0.0-beta.10/.12/.13/.14/.16)
     - Color-contrast violations: 132 → <see beta16 findings>
     - Heading-order violations: 88 → <see beta16 findings>

   ### Added
   - @axe-core/playwright integrated into comprehensive smoke (v2.0.0-beta.9)
   - scripts/a11y-detail-dump.ts for targeted axe analysis (v2.0.0-beta.12)
   - ProtectedRoute role-gating test coverage (v2.0.0-beta.6)
   - cmd/basement-server detectGarageVersion + tests (v2.0.0-beta.15)

   ### Known limitations
   - <list any a11y violations beta.16 left unfixed and why>
   ```

7. Write `/tmp/basement/rc1_readiness.md`:
   - One-line verdict (GO / NO-GO)
   - Pass/fail against each rc.1 criterion (table)
   - Smoke numbers + a11y breakdown
   - List of any failures + their probable cause
   - If NO-GO: which freshman cycle should fix it

8. **If GO**: tag and push.

   ```
   git -C /Users/mjackson/Developer/basement-ui add CHANGELOG.md
   git -C /Users/mjackson/Developer/basement-ui commit -m "v2.0.0-rc.1: release candidate — bundles beta.5..beta.16 fixes"
   git -C /Users/mjackson/Developer/basement-ui push origin HEAD:main
   git -C /Users/mjackson/Developer/basement-ui tag v2.0.0-rc.1
   git -C /Users/mjackson/Developer/basement-ui push origin v2.0.0-rc.1
   git ls-remote origin v2.0.0-rc.1
   ```

9. **If NO-GO**: commit just the CHANGELOG update if any, write
   findings, do NOT tag. Exit with **ESCALATE** noting which criteria
   failed.

## Anti-fabrication gates (NON-NEGOTIABLE)

- Every numeric / symbolic claim has a pasted-output citation
- Every existing-code reference cites `file:line`
- `git status --short` before commit
- `git -C <absolute-path>` always
- Stage by file name. NEVER `git add .` or `-A`
- NO `--no-verify` / `Co-authored-by` / `--force`
- DO NOT exit before pushing if you tagged — escalate if blocked

## What success looks like

- Smoke captured, results written
- CHANGELOG updated
- rc1_readiness.md with clear GO/NO-GO + numbers
- If GO: v2.0.0-rc.1 tag landed at main
- One DONE summary at end with: verdict, key metrics, commit sha (if tagged)

## What failure looks like

- Smoke breaks → **ESCALATE** with last 50 lines
- Live deploy doesn't reach beta.16 → **ESCALATE**, wait for senior
- All criteria pass but you don't tag → **ESCALATE** (operator wants
  rc.1 tagged when ready)

## Working repo

`/Users/mjackson/Developer/basement-ui`

## Mandatory ordering at the bottom (if GO)

```bash
git -C /Users/mjackson/Developer/basement-ui push origin HEAD:main
git -C /Users/mjackson/Developer/basement-ui push origin v2.0.0-rc.1
git ls-remote origin v2.0.0-rc.1
```
