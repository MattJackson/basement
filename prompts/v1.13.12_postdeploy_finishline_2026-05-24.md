You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui.

# Cycle: v1.13.12 — Postdeploy edge cases + 93 MINOR tap-target sweep

Two tracks. Both close the gap toward production-class smoke results.

## Track 1: Fix the 2 basement-code postdeploy fails

Per `docs/test-runs/2026-05-24_recovery.md`:

### 1a. Root redirect timeout
**Symptom**: `GET / → redirected to /login` — navigation timeout on root redirect when unauthed.
**Investigate**: `frontend/src/routes/index.tsx` (or wherever `/` is defined) + the protected-route logic in `frontend/src/shared/auth/ProtectedRoute.tsx`. Root probably does a synchronous fetch before redirect, taking >2s. Should redirect IMMEDIATELY on unauthed state (don't wait for any API call) — synchronous client-side branch.

### 1b. /files/keys heading mismatch
**Symptom**: Postdeploy expects heading "My Keys" (per v1.2.0d rename) but page renders something else.
**Investigate**: `frontend/src/routes/files/keys.tsx` (or `index.tsx` under that dir). What's the actual h1/h2? Either:
- Fix the page heading to "My Keys"
- OR update `scripts/postdeploy-ui-smoke.ts` if the rename meant a different intended heading

Pick based on operator intent: per v1.2.0d, the user-facing rename was "Region Keys" → "My Keys" for the user-tier keychain. Page should say "My Keys".

## Track 2: Sweep more tap-target MINORs (93 remaining)

v1.13.5 fixed 94 via the TanStack ErrorComponent swap. The remaining 93 are:
- Navigation links 30-31px height (per recovery doc)
- Action buttons 20-28px height

Most are likely in:
- `AppShell.tsx` nav links (the height isn't quite 44)
- DropdownMenuItem (small icon buttons inside menus)
- Table action buttons (per-row trash icon, copy-key icon, etc.)
- Status badge with onClick (if any are interactive)

Apply the v1.11.0.17 pattern (`min-h-[44px] min-w-[44px] sm:min-h-0 sm:min-w-0`) at the COMPONENT level for the highest-count offenders. Target: MINOR count <30.

## Hard constraints

- `pnpm build` + `go test -race ./...` green BEFORE push (run SYNCHRONOUSLY)
- `git commit <path>` pathspec form
- NO Co-Authored-By, NO `--no-verify`, NO emojis
- Per [[feedback_squash_commits]]: ONE commit covering both tracks
- Per [[feedback_freshman_must_merge_back_to_main]]: push HEAD:main FIRST, verify, then tag

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v1.13.12
git push origin v1.13.12
git ls-remote origin v1.13.12
```

## Acceptance

1. Root redirect timeout fix lands (instant client-side branch when unauthed)
2. /files/keys heading reads "My Keys"
3. Mobile-audit MINOR count drops by ≥50% (from 93 → ≤45)
4. Tag + main same commit; both pushed

Return summary AFTER both pushes succeed.

Estimated complexity: 60-90 min.
