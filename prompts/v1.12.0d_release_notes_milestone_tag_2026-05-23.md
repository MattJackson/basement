# v1.12.0d: Release Notes + Milestone Tag (CSK Envelope Encryption Story)

## Background
v1.12.0a/b/c implemented CSK (Cluster Secret Key) envelope encryption per ADR-0007. Need to document the full story and tag milestone.

## Task Requirements

### 1. Write Release Notes
Create `docs/release-notes/v1.12.0.md` documenting:
- **v1.12.0a**: CSK infrastructure (ClusterSecretManager, admin endpoints for lock/unlock)
- **v1.12.0b**: ADR-0007 compliance verification (federation uses UserRegion secrets, not admin_tokens/CSK)
- **v1.12.0c**: Backup runner lock-gate implementation and regression tests

Format should match existing release notes (see `docs/release-notes/v1.11.0.md`). Include:
- Headline summary
- Each sub-cycle's contributions
- References to ADR-0007, cycle reports in `docs/cycle-reports/`

### 2. Tag Milestone
After release notes are committed:
```bash
git tag v1.12.0
git push origin main
git push origin v1.12.0
```

## Verification Before Push
- `pnpm build` MUST be green
- `go test -race ./...` MUST be green  
- ONE commit only (squash all changes)
- Push HEAD:main FIRST, verify, then tag

## Anti-fabrication gates (NON-NEGOTIABLE)
- Every numeric / symbolic claim has a pasted-output citation
- Every existing-code reference cites `file:line`
- When the source doesn't say what you need, mark **OPEN** — DO NOT invent
- Don't reinterpret a prompt to fit available evidence; flag the gap
- `git status --short` before commit
- `git -C <absolute-path>` always; NEVER `cd <path> && git ...`
- Stage by file name: `git add path/to/file1 path/to/file2`. NEVER `git add .` or `-A`
- NO `--no-verify` on commits (even when no hook is installed)
- NO `Co-authored-by` text anywhere in the commit message
- DO NOT exit before pushing — if you can't push, return WITH a clear escalation reason

## Working repo
/Users/mjackson/Developer/basement-ui
