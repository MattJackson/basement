You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui.

# Cycle: v1.13.3 — Live verify morning fixes + capture React error #321 repro

The overnight bug-hunt op-tier shipped v1.13.1 / v1.13.1.1 / v1.13.2 / v1.11.0.31 but its smoke run flagged:
- Sev-2: WebDAV gateway OPTIONS/PROPFIND (likely Caddy edge config, not code)
- Sev-3: React error #321 in console (no specific repro captured)
- Sev-3: 217 tap-target MINOR warnings (cosmetic)

Verify the shipped fixes ACTUALLY work live + capture concrete React error #321 reproduction.

## Tasks

1. **Verify skin activation**: live on basement.pq.io (after Watchtower has rolled v1.13.2):
   - Login as matthew/password
   - Switch to admin view
   - Visit /admin/system → Skins section
   - In the redesigned 3-control UI, change "Default Skin" to `basement-95`
   - Verify the page re-skins LIVE (without manual refresh) within a few seconds
   - Toggle "User Overridable" on → off → on; verify Available Skins shows/hides
   - Set Default Skin back to `basement-default` before leaving (operator data hygiene)
   - Capture screenshots of the working 3-control UI for `docs/screenshots/v1.13.x/skins-redesign/`

2. **Verify double-toast fix**: 
   - Be in admin mode (elevated)
   - Click "Switch to user view" from UserMenu
   - Count toasts on screen — MUST be exactly 1 (not 2)
   - Repeat 3 times to confirm

3. **Capture React error #321 repro**:
   - Use Playwright with console listener
   - Walk every route in `frontend/src/routes/` and trigger `console.error` listener
   - When React error #321 fires, capture: URL, action that triggered it, full stack trace (or `componentStack`), surrounding React render path
   - Write findings to `docs/react-error-321-repro.md` with a numbered list of every fire-site
   - If you find an obvious root cause AND it's <15 lines to fix, fix it inline. Otherwise document for next cycle.

4. **Document the WebDAV Caddy issue**:
   - Read `internal/gateway/webdav/` + `docs/deployment/reverse-proxy.md`
   - Identify which Caddy directives are needed to pass OPTIONS + PROPFIND through to basement
   - Write `docs/operator-actions/webdav-caddy-required-directives.md` with the exact Caddyfile snippet matthew needs to add
   - NO code change — this is operator infra

## Hard constraints

- `pnpm build` + `go test -race ./...` green BEFORE push (run SYNCHRONOUSLY)
- `git commit <path>` pathspec form
- NO Co-Authored-By, NO `--no-verify`, NO emojis
- Per [[feedback_operator_data_off_limits]]: READ-only on operator data; revert any temporary skin change before exiting (operator hygiene)
- Per [[feedback_squash_commits]]: ONE commit covering all 4 tasks
- Per [[feedback_freshman_must_merge_back_to_main]]: push HEAD:main FIRST, verify, then tag

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v1.13.3
git push origin v1.13.3
git ls-remote origin v1.13.3
```

## Acceptance

1. Skin activation verified working (or escalated with diagnosis)
2. Double-toast verified fixed (or escalated)
3. React error #321 reproduction captured to docs
4. WebDAV Caddy directives documented
5. Tag + main same commit; both pushed

Return summary AFTER both pushes succeed.

Estimated complexity: 60-90 min.
