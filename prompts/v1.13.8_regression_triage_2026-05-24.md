You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui. URGENT — Sev-1 regression.

# Cycle: v1.13.8 — Regression triage between v1.11.0.31 (clean) and v1.13.7 (broken)

## What broke

Between v1.11.0.31 (morning baseline, postdeploy-ui-smoke 81/84 clean) and v1.13.7 (now), the FE regressed badly:
- postdeploy-ui-smoke: 9 pass / 30 fail / 10 skip (was 81/3)
- Frontend vitest: 41 failing tests (was ~0)
- Mobile audit: 124 React error #321 in console across all routes
- feature-smoke is the ONLY clean suite (BE didn't regress)

Tags shipped in the burst: v1.13.3, v1.13.4, v1.13.5, v1.13.6.

## Suspects

- **v1.13.4** (commit 8dc8012) — SkinsManager useState init refactor + useRef mounted flag for debounced save. Could break the hydration / initial-render path if the useRef pattern fires in an unexpected order.
- **v1.13.5** (commit 4403e47) — Swapped TanStack Router's default ErrorComponent for custom. If the custom component throws OR errors aren't propagated correctly, every route's error boundary triggers it → cascading React errors.
- **v1.13.6** (commit 4bd137f) — BE-only (Garage fanout), almost certainly not the culprit.
- **v1.13.3** (commit f943381) — docs-only, definitely not.

## Triage steps

1. **Run vitest locally + capture the first failing test's stack trace**:
   ```bash
   pnpm vitest --run 2>&1 | grep -A 20 "FAIL" | head -50
   ```
   This pinpoints which component is breaking + likely the culprit cycle.

2. **Git-blame the failing test's subject file**:
   - If most failures touch `system.tsx` (SkinsManager) → v1.13.4 is the bisect target
   - If most touch routes/error rendering → v1.13.5 is the bisect target

3. **Verify with revert-test**: `git stash; git checkout v1.11.0.31 -- <suspect-files>; pnpm vitest --run`. If tests now pass on the reverted file, the cycle's diff broke them.

4. **Fix the root cause**:
   - If v1.13.4 broke: the `useRef` mounted flag pattern may be racing with the React Query refetch. Likely fix: use `useEffect` with `[]` dep to set the mounted ref, OR drop the useRef entirely and gate the save on `caps.isSuccess && caps.data`.
   - If v1.13.5 broke: the custom ErrorComponent probably forgot to spread/handle the props TanStack passes. Restore the import + render the error info correctly.

5. **Verify the React error #321 mobile findings**:
   - If still widespread after the fix → there's another source we haven't found. Document + escalate.

6. **Re-run smoke harnesses**:
   ```bash
   pnpm vitest --run
   bash scripts/postdeploy-ui-smoke.sh   # only if dev server can target it
   ```
   Goal: vitest back to green, postdeploy back to >80% pass.

## Hard constraints

- `pnpm build` + `go test -race ./...` green BEFORE push (run SYNCHRONOUSLY)
- `git commit <path>` pathspec form
- NO Co-Authored-By, NO `--no-verify`, NO emojis
- Per [[feedback_squash_commits]]: ONE commit (the fix)
- Per [[feedback_freshman_must_merge_back_to_main]]: push HEAD:main FIRST, verify, then tag
- DO NOT exit before push — if you can't push, return WITH the explicit reason

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v1.13.8
git push origin v1.13.8
git ls-remote origin v1.13.8
```

## Acceptance

1. Root cause identified (commit + file:line)
2. Fix shipped: vitest green, postdeploy-ui-smoke back to >80% pass on the affected checks
3. Mobile audit's React #321 count drops significantly
4. Tag + main same commit; both pushed
5. Commit message names the regressing cycle + the fix

Return summary AFTER both pushes succeed.

Estimated complexity: 60-90 min — small fix once bisect locates it.
