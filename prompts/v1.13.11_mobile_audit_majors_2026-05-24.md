You are a freshman dev on basement-ui. Working dir: /Users/mjackson/Developer/basement-ui.

# Cycle: v1.13.11 — Fix the 6 MAJOR mobile-audit findings

v1.13.10 confirmed recovery from morning regression but flagged **6 MAJOR mobile-audit findings** still outstanding. These are blocking — MAJOR severity = horizontal scroll, broken layout, unreachable UI on phone viewports. Fix.

## Read first

- `docs/test-runs/2026-05-24_recovery.md` — the v1.13.10 recovery doc
- `docs/mobile-audit-2026-05-24.md` — the audit report (look at the MAJOR section for the 6 findings + file:line context)
- `scripts/mobile-audit.ts` — what the audit measures + how it classifies MAJOR vs MINOR
- v1.11.0.17 commit (AppShell overflow fix) — the pattern that fixed 51 MAJORs previously

## Approach

1. Run `bash scripts/mobile-audit.sh` (or the .ts directly) to refresh the current list with full repros (URL + viewport + offending element)
2. For each of the 6 MAJORs:
   - Identify root cause (horizontal scroll? missing min-w-0? off-canvas push?)
   - Fix at the component / layout level
3. Re-run the audit until MAJOR count = 0
4. Existing snapshots / tests must stay green; update only if visually intentional

## Common MAJOR patterns (from v1.11.0.17 paid-for episode)

- Header right-cluster pushed off-canvas → `min-w-0 flex-1` on left, `flex-shrink-0` on right
- Wide table without horizontal scroll wrapper → wrap in `overflow-x-auto` OR collapse to cards below sm
- Fixed-width modal/dialog wider than viewport → `max-w-[calc(100vw-1rem)]` on the dialog content
- Code blocks / monospace text not wrapping → `overflow-x-auto` on the container

## Hard constraints

- `pnpm build` + `go test -race ./...` green BEFORE push (run SYNCHRONOUSLY)
- `git commit <path>` pathspec form
- NO Co-Authored-By, NO `--no-verify`, NO emojis
- Per [[feedback_squash_commits]]: ONE commit
- Per [[feedback_freshman_must_merge_back_to_main]]: push HEAD:main FIRST, verify, then tag

## Mandatory ordering

```bash
git push origin HEAD:main
git ls-remote origin main
git tag v1.13.11
git push origin v1.13.11
git ls-remote origin v1.13.11
```

## Acceptance

1. Mobile-audit MAJOR count = 0 (was 6)
2. No new MAJORs introduced
3. Commit message lists the 6 findings + how each was fixed
4. Tag + main same commit; both pushed

Return summary AFTER both pushes succeed.

Estimated complexity: 45-90 min depending on root-cause depth.
