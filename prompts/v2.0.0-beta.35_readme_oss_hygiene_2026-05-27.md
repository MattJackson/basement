# v2.0.0-beta.35 — README polish + missing OSS files

## What you're producing

ONE commit, ONE tag. Bring the repo's public surface to world-class
OSS standard. Two parts:

### Part A — Add missing OSS files

1. **`CODE_OF_CONDUCT.md`** — adopt **Contributor Covenant v2.1**
   verbatim. Source: <https://www.contributor-covenant.org/version/2/1/code_of_conduct.md>.
   Set the contact email to `matthew@pq.io` (same as SECURITY.md).
   Do NOT modify the Covenant text itself beyond the contact email.

2. **`SUPPORT.md`** (root) — short doc telling users where to get help.
   Sections:
   - **Bugs**: file an issue using the bug-report template
   - **Feature requests**: feature-request template
   - **Questions**: GitHub Discussions if enabled, else the question
     issue template
   - **Security**: link to SECURITY.md (do not file in public)
   - **Commercial / dual-licensing**: matthew@pq.io
   Keep it under 80 lines. Tone matches CONTRIBUTING.md.

3. **`.github/FUNDING.yml`** — sponsorship config. Use the
   `github: [MattJackson]` form. If the operator wants a Buy Me a
   Coffee / Open Collective / Ko-fi later, they can add — for now
   just the GitHub Sponsors entry. (If you can't find a Sponsors
   page for MattJackson, just create the file with the entry; GitHub
   will accept it and show the link as configurable.)

4. **`CODEOWNERS`** at repo root — single line:
   ```
   * @MattJackson
   ```
   With a leading comment block explaining that all changes default
   to maintainer review and that area-owners will be added as the
   project grows.

### Part B — README updates

Edit `README.md` to reflect v2.0.0 + add a few world-class polish bits.

#### B1. Refresh the roadmap section

Replace `**v1.x is complete.**` with text that acknowledges v2.0 is
out (or in RC, depending on whether v2.0.0 has been tagged at the time
you're editing — check `git tag -l | tail -5` to see). Add a v2.0 row
to the minor-history table:

```
| v2.0 | Clean break: drop legacy JWT encryption, OIDC subject, SkinPolicy, WebDAV path-style; multilingual i18n; role-aware nav + persona pill role switcher; user-tier skin persistence; B1 cluster auto-detect (garage v1 vs v2); accessibility pass (-71% violations) |
```

Replace the "What's next is open" paragraph with something current —
either ROADMAP open or pointing to GitHub Discussions / issues.

#### B2. Add a "Getting help" / "Community" section

Just after the "Documentation" section and before "Contributing".
Three or four lines:
- File a bug → bug_report.yml link
- Ask a question → question.yml or Discussions
- Security → SECURITY.md
- Chat (if there's a Discord / Matrix / IRC) — leave OPEN and add
  a TODO comment if the operator hasn't shared one.

#### B3. SemVer + stability commitment

Add a small "Stability" or "Versioning" subsection near the License
section:
```
## Versioning

basement follows [Semantic Versioning](https://semver.org/) for
its public API:

- **Major** (e.g. v1.x → v2.0): breaking changes — see CHANGELOG
- **Minor** (e.g. v2.0 → v2.1): new features, backwards-compatible
- **Patch** (e.g. v2.0.0 → v2.0.1): bug fixes only

CHANGELOG.md lists every release. Use the `latest` Docker tag for
the rolling line, or pin to a specific tag (e.g. `:v2.0.0`) for
production reproducibility.
```

#### B4. Refresh the comparison table dates if obvious

The comparison says "verified ... on 2026-05-23". If you don't have
new sourcing handy, leave the date alone — DO NOT invent new
"verified" claims. If you DO want to update a row, cite the source
in `docs/comparison-research.md` (extend that file with the same
formatting).

For "Latest release / activity" — basement's row should now read
something like `v2.0.0-rc.2 (2026-05) or vX.Y.Z (date)`. Check the
latest tag with `git -C /Users/mjackson/Developer/basement-ui describe --tags --abbrev=0` and use that value.

#### B5. Tiny visual polish

- The hero image at the top — leave alone unless broken.
- Verify all `docs/...` links resolve (`ls docs/` from the repo root).
  If any link is broken, fix it or remove it.

### What NOT to do

- Do NOT rewrite huge sections of the README. The current content is
  good. Edit in place, append new sections, don't restructure.
- Do NOT remove the comparison table or claim feature parity that
  isn't backed by sources.
- Do NOT add badges that don't link to real things (e.g. no Discord
  badge if there's no Discord).
- Do NOT invent a roadmap for v2.x — leave "what's next" open per
  Rule #15 (don't pre-announce future versions).
- Do NOT add `Co-Authored-By` or any AI attribution to commits or PR
  descriptions.

## Anti-fabrication gates (NON-NEGOTIABLE)

- Every numeric / symbolic claim has a pasted-output citation
- Every existing-code reference cites `file:line`
- When the source doesn't say what you need, mark **OPEN** — DO NOT invent
- `git status --short` before commit
- `git -C <absolute-path>` always
- Stage by file name. NEVER `git add .` or `-A`
- NO `--no-verify` / `Co-authored-by` / `--force`
- ONE commit + ONE tag this cycle
- DO NOT exit before pushing — escalate if blocked

## Process

1. `git -C /Users/mjackson/Developer/basement-ui pull --ff-only origin main`
2. Add new files:
   - `CODE_OF_CONDUCT.md` (Contributor Covenant 2.1 verbatim, email=matthew@pq.io)
   - `SUPPORT.md`
   - `.github/FUNDING.yml`
   - `CODEOWNERS`
3. Edit `README.md` per B1-B5.
4. Verify all links: `grep -oE '\(\.?\/[^)]+\)' README.md | sort -u`,
   then check each path exists.
5. `cd /Users/mjackson/Developer/basement-ui/frontend && pnpm build && pnpm test:run` — should still pass (no code touched but verify).
6. Stage explicit files. ONE commit:
   `v2.0.0-beta.35: README polish + add CODE_OF_CONDUCT / SUPPORT / FUNDING / CODEOWNERS`
7. Push HEAD:main, tag, push tag, ls-remote verify.

## What success looks like

- 4 new files added (CODE_OF_CONDUCT, SUPPORT, FUNDING.yml, CODEOWNERS)
- README updated with v2.0 in the roadmap + Getting help + Versioning sections
- Comparison table's "latest release" row reflects current tag
- All links resolve
- ONE commit, ONE tag (`v2.0.0-beta.35`)

## What failure looks like

- Broken doc link in README → fix or remove
- Hero image path broken → leave the file alone, just note in DONE
- Github Sponsors page not configured → still create FUNDING.yml; GitHub
  surfaces a "Set up sponsors" prompt in the UI

## Working repo

`/Users/mjackson/Developer/basement-ui`

## Mandatory ordering at the bottom

```bash
git -C /Users/mjackson/Developer/basement-ui push origin HEAD:main
git -C /Users/mjackson/Developer/basement-ui push origin v2.0.0-beta.35
git -C /Users/mjackson/Developer/basement-ui ls-remote origin v2.0.0-beta.35
```
